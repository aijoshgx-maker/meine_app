<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Für die Fällig-Erinnerung (flutter_local_notifications): Anzeigen
         auf Android 13+ und Überleben eines Geräte-Neustarts. Keine
         Exact-Alarm-Berechtigung nötig, da inexactAllowWhileIdle genutzt wird. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- P10c: android.permission.DUMP taucht in keiner der aktuellen
         Quell-Manifeste auf (auch nicht in debug/profile) - vermutlich kam
         sie ursprünglich aus einer inzwischen entfernten Debug-Abhängigkeit.
         Defensiv trotzdem ausschließen, falls sie über eine künftige
         Abhängigkeit zurückkehrt. -->
    <uses-permission android:name="android.permission.DUMP" tools:node="remove" />
    <!-- Für das Diktieren der Antwort im Fachgespräch. Die Erkennung selbst
         läuft im Systemdienst des Geräts, nicht in dieser App - deshalb
         braucht die App weiterhin keinen Internetzugriff. Die Berechtigung
         wird erst abgefragt, wenn man den Diktierknopf drückt. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <application
        android:label="AP2 Trainer"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Ohne das findet speech_to_text ab Android 11 den
             Spracherkennungsdienst nicht und meldet "nicht verfügbar". -->
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>
</manifest>
